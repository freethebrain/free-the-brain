{
  "name": "@ftb/core",
  "version": "0.1.0",
  "private": true,
  "description": "Registry parser, delta applier, serialiser, date semantics and queue derivation shared by every Free the Brain surface.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --project core",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
